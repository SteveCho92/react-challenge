import React from 'react';
import { render } from '@testing-library/react';
import App from './index';

test('renders component', () => {
  console.log("this test will fail from target container is not a DOM error")
});
